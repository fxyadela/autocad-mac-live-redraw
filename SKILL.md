---
name: autocad-mac-live-redraw
description: 在 macOS 的本机 AutoCAD 中，把一张或多张已校准二维重绘清单按图元逐一画成可编辑 CAD 图形；多图时按上传顺序每张新建独立画布，全部完成后才结束，并默认保持未保存。也可按用户明确要求将现有 DXF 另存为 DWG。适用于豆包工作等具备本地电脑操作能力的代理；不适用于 Windows COM 自动化。
---

# AutoCAD for Mac 本机重绘

这是自包含的 **Mac 原生适配**，无需安装其他 CAD 技能；不调用 `win32com`、`pywin32`、`taskkill`。豆包或其他代理执行本技能，须另有本地电脑操作能力且当前 Mac 的 AutoCAD 可交互；技能文字和离线文件本身不会自动获得桌面权限。目前脚本通过离线测试，合成小样已在 AutoCAD 2027 for Mac 实机完成逐图元绘制；复杂正式图纸和 DWG 交付仍须逐次验证。

## 调用者只描述目标

调用者只需提供一张或多张原图/清单，并说明要“从空白画布逐图元绘制”或“保真导入”。多图任务默认按上传顺序执行，一张原图对应一个新的 AutoCAD 图纸标签，全部完成后才结束。逐图元绘制默认**不保存、不另存、不关闭**；只有调用者明确要求 DWG 文件时才进入保存流程。不得要求调用者了解或在请求中写入内部数字命令、`CADLIVE`、`type_text`、Bundle 路径、LSP 加载、保存命令或延时参数；这些全是本 Skill 的内部执行细节。当需要向调用者提供“调用指令”时，只保留业务目标、输入图纸和交付要求。

## 首先确定是哪一种交付

- 用户要看到从空白画布逐步画出可编辑对象：按 [输入规范](references/spec.md) 从原图建立证据约束、尺寸校准、坐标/图层/图元的 JSON 清单，再用 `scripts/compile_mac_redraw.py` 生成本机 AutoLISP，进入“原生逐步绘制”流程；完成后默认留在未保存画布，方便继续观看或追加修改。
- 用户已有完整 DXF，优先保真：用本机 AutoCAD 的图形界面打开 DXF，再 **另存为新的 DWG**，关闭后重开检验。DXF 本身也包含可编辑 CAD 图元，但打开现成 DXF 不是从空白逐笔画的演示。
- 不要把 STEP/机械建模或 QCAD 的 JS `-exec` 当成 AutoCAD for Mac 的原生操控方式。

## 原图到清单（两条路线共同前提）

1. 读取原图和可辨尺寸，记录来源、比例依据、单位、模糊项。无可靠尺度时用 `unitless`；不要把像素直接宣称为毫米。先转录文本/尺寸，再重建墙、门、窗、轴网、房间文字和标注的 CAD 坐标。图纸所含的表格、填充等复杂内容也须记录，不得为了让程序成功而漏掉。
2. 优先复用用户现有的高质量 DXF/结构化 JSON，检查图元数量、类型、层和尺寸，以及图片对照；不能凭“DXF 有实体”推断和原图完全一致。
3. 每个清单图元均应注明其证据（已知、校准缩放、推断、不可辨）。具体可执行字段和局限见本技能的 [输入规范](references/spec.md)；不要依赖用户另装其他技能才能运行。

## 生成原生可见绘制脚本

从技能目录运行（不要替换现有文件）：

```bash
python3 scripts/compile_mac_redraw.py --spec /绝对路径/重绘清单.json --out /绝对路径/新图绘制.lsp --delay-ms 35
```

编译阶段先严格核验字段、非有限数字、图层、单位、所有类型；仅支持 `line`, `polyline`, `rectangle`, `circle`, `arc`, `text`, `mtext`, `leader`（线段加文字，并非原生 LEADER）, `linear_dimension`, `aligned_dimension`, `center_mark`。`table` 和 `radial_dimension` 等当前不支持，编译会失败；需要这些内容时选择完整 DXF 导入路线或进一步实现适配，不能只画几根线就说图纸完成。`text_style`/非连续线型/复杂尺寸样式未在此编译器实现，遇到明确请求亦会失败。主路径会在开始前定位整张图的视口，再按清单顺序每创建一个原生图元立即显示；`--delay-ms` 控制每个图元后的短暂停顿，推荐 25–40 毫秒以兼顾流畅度和可见性。默认无自动覆盖、清空或保存。

编译后从技能目录在后台部署当前脚本：

```bash
python3 scripts/deploy_autocad_bundle.py --lsp /绝对路径/新图绘制.lsp
```

它把当前 LSP 放入当前用户的 AutoCAD `ApplicationAddins/AutoCADMacLiveRedraw.bundle`，保留已经验证过的单键绘制入口 `1`；脚本加载后还会提供内部单键 `0` 来新建下一张图。完整命令 `CADLIVE` 和无停顿核验命令 `CADFAST` 只作人工备用。部署输出会明确标记 `BUNDLE_INSTALLED`、`BUNDLE_UPGRADED` 或 `BUNDLE_UPDATED`；此目录只用于本技能自己的 bundle，不修改 `SECURELOAD`、`TRUSTEDPATHS` 或 Autodesk 安装目录。

## 多张原图批量绘制

1. 先按上传顺序编号并清点所有原图。每张图分别完成识图、校准和 JSON 清单；在切换 AutoCAD 前，先确认这一批每张图都能通过编译。不得画完第一张就把任务判为完成，也不得静默跳过失败或模糊的图片。
2. 两至九张图使用批量编译器，`--spec` 按上传顺序重复传入：

```bash
python3 scripts/compile_mac_batch.py \
  --spec /绝对路径/第1张.json \
  --spec /绝对路径/第2张.json \
  --out /绝对路径/本批次绘制.lsp \
  --delay-ms 35
```

3. 超过九张时按上传顺序分成每组最多九张；完成一组后在后台部署下一组，继续新建图纸，直到所有组完成。每个批次使用独立状态标识，避免旧任务进度污染新任务。绘制和换图命令、状态标识及 bundle 部署均为内部细节，不写给调用者。
4. 批量脚本只把多张图的绘制命令放进同一个 bundle，不会自动保存、覆盖或关闭任何图纸，也不会未经检查自行跳到下一张。它还记录上一张完成时所在的图纸标签：没有真正切到新标签时会拒绝绘制下一张，已经切换后再次误触换图也会被拒绝。

## 豆包在 Mac AutoCAD 的实际操作

1. 在切换到 AutoCAD 前，先在后台完成识图、清单校验、LSP 编译和 bundle 部署；不要让用户观看代理在文件窗口中反复找文件。确认豆包的本地电脑/操作电脑功能实际可用且 AutoCAD 主界面/许可状态正常。若原生应用打不开、弹许可窗口、指令无响应，先报告这个阻断；进程存在 ≠ 绘图画布可用。不得用静态预览伪装应用操作。
2. 严格按部署输出处理。`BUNDLE_UPDATED`：不退出、不重启 AutoCAD，直接新建空白图。`BUNDLE_INSTALLED` 或 `BUNDLE_UPGRADED`：AutoCAD for Mac 没有 `_APPAUTOLOADER` 命令；若 AutoCAD 已打开，在正式绘图任务开始前关闭并正常启动一次，让新命令清单被发现；若尚未打开，正常启动即可。这是安装/升级后的单次准备，不是失败恢复；任务中不得再反复重启。不要打开 `APPLOAD`，也不要查看“已加载的应用程序”列表。
3. 整个绘图阶段必须留在 AutoCAD；不得按 F1、点击帮助/问号、打开或切换浏览器，也不得在没有看见弹窗时猜测“安全确认正在等待”并全屏搜索。若意外出现浏览器或 Autodesk 帮助页，立即停止并报告误触，不得返回 AutoCAD 继续重试。
4. 先看命令栏是否为空闲的“键入命令/Command:”。若仍显示 `CIRCLE`、`SAVEAS` 或其他参数提示，只按一次 Escape；未回到空闲状态就停止报告。空闲后单击输入区一次，单图或批量第一张都用 `type_text` 只输入单个字符 `1`，再按 Return 一次。数字命令是专为桌面操作长文本被截断而注册的内部入口；不得通过 `type_text`/`set_value` 输入 `CADLIVE`，否则只送入首字母 `C` 时会误启动 `CIRCLE`。不要输入以 `(` 开头的 AutoLISP 表达式。若数字一次执行后仍未出现加载信息和首个图元，停止并报告命令栏原文；不得重启、打开 APPLOAD、搜索文件、检查安全弹窗或再次提交命令。
5. 单键 `1` 会读取批次内部进度，在当前新画布绘制上传顺序中的对应图纸；每个图元应在已定位的画布上一个接一个出现。`CADLIVE` 只作为人工键盘的完整名称备用，`CADFAST` 仅用于无停顿核验。不要通过打开预先生成的 DWG/DXF 冒充逐一绘制。脚本拒绝在已有模型空间对象的图中重复执行。中文字符依赖当前 AutoCAD 文字引擎及字体，视觉复查必须包含中文、尺寸箭头和图层。
6. 看见命令栏的完成信息后，检查画布对象数和进度；若有 `CADREDRAW FAILED` 或中途停止，不保存部分图纸，不要声称成功。核对原图的重要尺寸、墙线门窗、文字/表格/填充，不合格则返修清单再重画。若旧 DXF 路线，在 UI 打开现有 DXF 并看其全部内容，而不是打开自动生成的演示预览。
7. **批量换图：**当前图核对完成后，不保存也不关闭；若还有下一张，命令栏空闲时输入内部单键 `0` 并按 Return，由 `QNEW` 新建独立空白图纸。确认新画布确实为空，再输入单键 `1` 并按 Return；脚本会根据批次进度自动选择下一张。上一张必须保留在自己的未保存图纸标签中。`0` 未识别、弹出模板/保存对话框或新画布不为空时立即停止并报告，不得再点击界面“新建”按钮、重复部署单图脚本、重启或覆盖当前图来继续。
8. **默认停止条件：**只有最后一张也完成核对后才结束任务。停留在最后一张 AutoCAD 画布，不按 `Cmd+S`，不调用 `SAVE`/`QSAVE`/`SAVEAS`，不打开保存窗口，不关闭或重开任何图纸。向调用者报告“共 N 张全部绘制完成，N 张均未保存”，然后停止操作。不得把第一张完成或“可能需要交付文件”自行解释为结束或保存授权。
9. 只有调用者明确说“保存”“另存为 DWG”或提供保存路径时，才进入保存流程；批量任务须明确说明保存一张还是全部。保存时使用新的绝对路径，不覆盖源文件；如调用者同时要求验收，再关闭重开并实际选中/修改图元。
10. 汇报分开列：上传总数、已完成/未完成序号、每张清单校准与原图准确度、AutoCAD 实际逐步绘制、每张当前保存状态，以及用户明确要求后才执行的 DWG 保存/重开检验。任何一张失败就给出准确进度并停止，不能把部分完成说成全部完成。

## 使用边界

AutoCAD for Mac 官方支持 AutoLISP（`entmake`、`command-s`），不支持 Windows ActiveX/COM 工作流。编译器是当前明确可测试的 Mac 桥接部分，不是对任意扫描图的自动识图工具；完整的图纸重绘仍需要可信的测绘/文字核对。不能加载 LSP 的安全弹窗和应用不可交互是执行前提的阻断，不能绕过系统权限。
